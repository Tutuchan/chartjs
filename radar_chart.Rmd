---
title: "Radar Charts"
---

```{r, warning=FALSE, error=FALSE, message=FALSE}
library(chartjs)
library(magrittr)
chartjs(data = list(mpg = mtcars$mpg, cyl = mtcars$cyl), labels = row.names(mtcars)) %>% 
  radarChart
```


